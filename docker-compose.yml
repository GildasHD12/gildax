version: '3.8'

services:
  zayamarket:
    build: .
    ports:
      - "3000:3000"
    environment:
      VITE_GEMINI_API_KEY: ${VITE_GEMINI_API_KEY}
      VITE_VEO_API_KEY: ${VITE_VEO_API_KEY}
    volumes:
      - .:/app
      - /app/node_modules